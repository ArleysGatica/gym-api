services:
  - type: web
    name: backend-gym
    env: node
    plan: free
    branch: main
    buildCommand: npm run build
    startCommand: npm run start:prod
    rootDir: .
    envVars:
      - key: MONGO_URI
        sync: false
      - key: MONGO_LOCAL_URI
        sync: false
      - key: MONGO_DB_NAME
        value: gymdb
      - key: NODE_ENV
        value: production
      - key: JWT_SECRET
        sync: false
      - key: JWT_EXPIRES_IN
        value: 1h
